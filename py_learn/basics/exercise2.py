a=b=c=10
e,f,g=1,"test",2.0

print(a,b,c,e,f,g)

A = 50
B = 75

# Adding two numbers A+B
result = A + B

# printing the values of two Numbers
print("Result of {0} and {1} is {2}" .format(A, B, result))
#here we set the format of the numbers as well
#format() is one of the string formatting methods in Python3,
# which allows multiple substitutions and value formatting.
# This method lets us concatenate elements within a string through positional formatting.

